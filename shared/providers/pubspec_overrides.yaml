# melos_managed_dependency_overrides: app_logger,config,service,storage
dependency_overrides:
  app_logger:
    path: ../logger
  config:
    path: ../config
  service:
    path: ../service
  storage:
    path: ../storage
