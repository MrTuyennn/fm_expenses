# melos_managed_dependency_overrides: storage,app_logger,service,config
dependency_overrides:
  app_logger:
    path: ../logger
  config:
    path: ../config
  service:
    path: ../service
  storage:
    path: ../storage
